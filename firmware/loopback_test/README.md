# 음향 폐루프 자기진단 — 그리고 오디오가 막힌 이유

사람이 보거나 듣지 않아도 오디오 경로를 판정한다. 보드가 스스로 음을 내고 자기
마이크로 들으면 되기 때문이다.

핵심은 **동기 검파**다. 낸 주파수의 sin/cos 과 곱해 적분하면 노이즈 플로어보다 훨씬
아래의 신호도 잡힌다. 그냥 RMS 를 보면 "조용해서 안 들림" 과 "경로가 죽음" 이
구분되지 않는다 — 실제로 이 함정에 한 번 빠졌다(같은 설정이 1초 측정에서 피크 63,
400ms 에서 피크 1).

## 결론: I2S 오디오가 흐르지 않는다

```
[1] 기준선 (무음)        검파 L 0.00  R 0.00   RMS L 1.0  R 1.0
[2] 음 출력, GPIO22=LOW  검파 L 0.00  R 0.00   RMS L 1.0  R 1.0
[3] 음 출력, GPIO22=HIGH 검파 L 0.00  R 0.00   RMS L 1.0  R 1.0
```

RMS 1.0 은 LSB 노이즈 수준, 즉 완전한 0 이다. `mic_node` 에서 관측된 "L 피크 63" 은
실제 오디오가 아니라 순간 노이즈였다.

## 소프트웨어로 시도한 것 전부

| 시도 | 결과 |
| --- | --- |
| AC101 I2C 통신 | **정상** — 레지스터 쓰기·읽기 양방향 확인 |
| ADC 경로 레지스터 | **정상 설정** — `DIG=0x8000 APC=0x3BC0 SRC=0x2020` (읽기로 검증) |
| I2S DIN 후보 8개 (35/34/39/36/23/22/19/18) | GPIO35 만 "구동되는 0", 나머지는 플로팅 노이즈 |
| `ADC_VOL_CTRL` 후보 6개 | 전부 무신호 (기본값이 이미 뮤트 아님) |
| 샘플레이트 44100 (PLL 일치) | 무신호 |
| `ADC_SRC` 후보 9개 | 전부 무신호 |
| `ADC_APC_CTRL` 후보 9개 | 전부 무신호 |
| `MODE_ADC` / `MODE_ADC_DAC` | 둘 다 무신호 |
| `use_apll` on / off | 둘 다 무신호 |
| 라이브러리 `MODE_ADC` 미완성 버그 수정 | 필요했지만 이것만으로는 부족 |

**코덱과는 완벽하게 대화가 되는데 오디오 데이터만 안 흐른다.** 이 조합이 남기는
결론은 하나다.

## 정정: DIN 은 GPIO34 가 아니라 GPIO35 다

한때 GPIO34 로 "실측 정정" 했다가 되돌렸다. **그 판정이 틀렸다.**

GPIO34 가 피크 63 을 내서 신호로 봤지만, 그건 **플로팅 입력의 노이즈**였다.
자기 검증(DIN 을 DOUT 에 붙이기)에서 값이 갈리며 판별법이 드러났다:

| 읽은 값 | 의미 |
| --- | --- |
| RMS **0.0** (깨끗한 0) | 누군가 그 라인을 **실제로 구동**한다 → 연결됨, 내용이 디지털 무음 |
| RMS **1.0** (노이즈) | 아무도 안 붙었다 → **플로팅** |

GPIO35 는 정확히 0.0, GPIO34 는 1.0 이었다. 즉 **GPIO35 가 연결된 라인**이고,
코덱은 유효한 I2S 프레임을 보내는데 그 내용이 무음이다.

이 구분(0.0 vs 1.0)이 이 세션에서 가장 쓸모 있는 진단 도구였다. "피크가 0 보다 크다"
로 신호를 판정하면 플로팅 핀에 속는다.

## 코덱 쪽도 전부 확인했다

`begin()` 이 이미 필요한 것을 다 쓴다 — 내가 "빠졌다" 고 본 것들이 실은 다 있었다:

```cpp
WriteReg(PLL_CTRL1, 0x014f);  WriteReg(PLL_CTRL2, 0x8600);  // 256×44.1kHz MCLK
SetI2sSampleRate(SAMPLE_RATE_44100);
SetI2sClock(BCLK_DIV_8, false, LRCK_DIV_32, false);
WriteReg(ADC_SRCBST_CTRL, 0xccc4);   // 마이크 부스트
WriteReg(ADC_SRC,         0x2020);   // 마이크 라우팅
WriteReg(ADC_DIG_CTRL,    0x8000);   // ADC 디지털
WriteReg(ADC_APC_CTRL,    0xbbc3);   // 아날로그 경로
```

여기서 내가 두 개를 스스로 깨뜨렸다가 고쳤다:

1. **샘플레이트를 16k/32k 로 바꿨다.** PLL 이 256×44.1kHz MCLK 를 전제하는데 ESP32 는
   MCLK 을 256×샘플레이트로 낸다. 32kHz 면 8.192MHz 를 주고 코덱은 11.2896MHz 를
   기대해 PLL 이 락하지 못한다. → 44100 으로 고정해야 한다.
2. **`ADC_APC_CTRL` 을 0xbbc3 → 0x3bc0 으로 덮어썼다.** `MODE_LINE` 값을 베낀 것인데
   상위 비트가 ADC L/R 인에이블로 보인다. 즉 ADC 를 직접 껐다. `begin()` 값이 맞다.

둘 다 고쳤고 **여전히 무음이다.** `ADC_VOL_CTRL(0x41)` 도 확인했다 — begin() 이 안
쓰지만 기본값이 이미 `0xA0A0`(뮤트 아님)이고, 6개 값을 순회해도 변화가 없다.

## 남은 용의자: 물리 쪽

`docs/HARDWARE.md` 에 첫 조사부터 적어 둔 미확인 항목이다 — 여러 기능이 공유하는
GPIO 를 microSD 쪽으로 붙일지 다른 쪽(JTAG/키)으로 붙일지 갈라주는 물리 점퍼이고,
리비전마다 매핑이 다르다. 이게 I2S 또는 마이크 라인을 끊어놓으면 **정확히 이 증상**이
난다: I2C 는 멀쩡하고(별도 버스) 오디오만 0.

물리 스위치라 코드로 바꿀 수 없다.

## 그래서 무한 감시로 남겨 뒀다

이 펌웨어는 1초에 한 번씩 폐루프 테스트를 반복하며 로그를 찍는다. **아무 때나 DIP
스위치를 딸깍거리면 오디오가 살아나는 순간이 로그에 남는다** — 지켜볼 필요가 없다.

```
[  12] 검파 L    0.00 R    0.00  RMS L    1.0 R    1.0  무신호 (DIP 스위치를 딸깍거려 보세요)
...
*** 오디오가 살아났다! DIP 스위치가 원인이었다 ***
[  47] 검파 L  812.40 R  790.11  RMS L  980.2 R  965.7  ◀◀ 신호 있음
```

읽는 방법:

```bash
python3 - <<'PY'
import serial, time
s = serial.Serial('/dev/ttyUSB0', 115200, timeout=0.3)
t = time.time(); buf = b''
while time.time() - t < 10: buf += s.read(4096)
print(buf.decode('utf-8', 'replace'))
PY
```

## 이 보드에서 확인된 것 / 안 되는 것

**되는 것** — ESP32 자체는 온전하다. PSRAM 4MB, 플래시 4MB, 듀얼코어, WiFi, BLE,
I2C(GPIO32/33), GPIO, 그리고 온디바이스 추론(numpy 참조와 오차 1.27e-07 일치).

**안 되는 것** — I2S 오디오 입출력. 그래서 스피커·마이크를 쓰는 프로젝트는 전부 막힌다:
번역 무전기(`cabin_node`), 악기(`synth_node`), 2마이크 센서(`mic_node`).

세 펌웨어 모두 빌드·플래시·수치 검증까지는 끝나 있다. 오디오 배선만 풀리면 바로 돈다.
